#!/bin/bash

if [ -z "$1" ]
then
    #/usr/bin/emacsclient -t  --socket-name=$HOME/.emacs.d/cache/emacs-server-file
    emacsclient -t  
else
    #/usr/bin/emacsclient -t  --socket-name=$HOME/.emacs.d/cache/emacs-server-file "$@"
    emacsclient -t   "$@"
fi

